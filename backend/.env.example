DATABASE_URL=postgresql://lacerta:lacerta@localhost:5434/lacerta
PORT=4001
NODE_ENV=development
CORS_ORIGIN=*

# Claude API — required for AI plan generation
ANTHROPIC_API_KEY=sk-ant-...

# JWT EdDSA keys — optional in development (auto-generated), required in production
# JWT_PRIVATE_KEY=
# JWT_PUBLIC_KEY=
