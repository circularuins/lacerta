import 'package:flutter/material.dart';

// Primary color — inspired by the Lacerta constellation (deep indigo/violet)
const colorSeed = Color(0xFF6366F1);

// Surface colors
const colorSurface = Color(0xFF0F0F23);
const colorSurfaceVariant = Color(0xFF1A1A2E);
const colorCard = Color(0xFF16213E);

// Accent
const colorAccent = Color(0xFF818CF8);
const colorSuccess = Color(0xFF34D399);
const colorWarning = Color(0xFFFBBF24);
const colorError = Color(0xFFEF4444);

// Text
const colorTextPrimary = Color(0xFFF1F5F9);
const colorTextSecondary = Color(0xFF94A3B8);
const colorTextMuted = Color(0xFF64748B);

// Spacing
const spacingXs = 4.0;
const spacingSm = 8.0;
const spacingMd = 16.0;
const spacingLg = 24.0;
const spacingXl = 32.0;

// Border radius
const radiusSm = 8.0;
const radiusMd = 12.0;
const radiusLg = 16.0;
